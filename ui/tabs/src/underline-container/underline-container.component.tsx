import styled         from '@emotion/styled'

import { motion }     from 'framer-motion'

import { baseStyles } from './underline-container.styles'

const UnderlineContainer = styled(motion.div as any)(baseStyles)

export { UnderlineContainer }
